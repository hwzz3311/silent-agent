# Silent Agent 依赖

# 核心依赖
websockets>=12.0
aiohttp>=3.9.0
pydantic>=2.0.0
fastapi>=0.109.0
uvicorn>=0.27.0

# Puppeteer 支持（可选，用于真实无障碍树）
puppeteer>=7.0.0
puppeteer-extra>=3.0.0
puppeteer-extra-plugin-stealth>=2.9.0

# 开发依赖
pillow>=10.0